<%= javascript_tag do %>
    $.ajax({
    type: "GET",
    cache: false,
    url: "<%= row_site_path(site,{group: @group}) %>",
    dataType: "html"
    })
    .success(function( data ) {
    $( "#site<%= site.id %>" ).after( data );

    });
<% end %>

