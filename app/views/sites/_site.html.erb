<%= javascript_tag do %>
    $.ajax({
    type: "GET",
    cache: false,
    url: "<%= violators_site_path(site) %>",
    dataType: "html"
    })
    .success(function( data ) {
    $( "#tbody<%= site.id %>" ).html( data );

    });
<% end %>